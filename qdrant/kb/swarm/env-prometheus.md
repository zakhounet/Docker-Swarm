# Prometheus — Variables d'environnement

Stack : `prometheus/docker-compose.yml`
Fichier : `prometheus/.env` (gitignored)

## Variables

| Variable | Rôle |
|---|---|
| `PVE_TOKEN_VALUE` | Token API Proxmox pour le pve-exporter (scrape des métriques des nœuds Proxmox) |
